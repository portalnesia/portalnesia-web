import API from 'portal/utils/axios'
import crypto from 'portal/utils/crypto'

export default async function ChangeProfile(id: string){
    /*if(!id) throw new Error("ID cannot be empty");
    const dt = await API.post("/")
    return new Promise((res,rej)=>{
        

        PNpost(`${process.env.API}/setting/change_profile`,{id:id}).then((result)=>{
            if(result.error) return rej(result.msg);
            else return res(result.msg)
        }).catch((err)=>{
            return rej(err?.msg||"Something went wrong");
        })
    })*/
}